interface:
  display_name: "Personal Growth Strategy"
  short_description: "Choose one growth focus and build compounding work"
  default_prompt: "Use $formulate-strategy to identify the one field, project, or startup worth concentrating on now and build a compounding path around it."
policy:
  allow_implicit_invocation: true
